/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['daniella.blog-s.de'], // Add your domain here
    formats: ['image/webp'],
  },
};

export default nextConfig;
